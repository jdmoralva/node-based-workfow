# Archived Snapshot Notes

This directory is not part of the approved baseline workflow for legacy visual parity.

- Approved desktop legacy baselines live in `apps/web/tests/visual/baselines/legacy/`.
- Temporary comparison artifacts live in `apps/web/tests/visual/artifacts/`.
- Any older mobile or mixed-viewport images previously stored here are obsolete archive material and must not be used as approved references.

Reviewer guidance and route mapping live in `specs/004-legacy-visual-parity/contracts/desktop-visual-baselines.md`.
