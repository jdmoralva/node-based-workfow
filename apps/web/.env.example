# Default local flow: server requests use apps/api while browser requests use
# same-origin Next.js proxy routes.
API_BASE_URL=http://127.0.0.1:8000

# Set this only when apps/api allows credentialed CORS from the web origin.
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

AUTH_SESSION_COOKIE_NAME=rv_session
E2E_AUTH_USERNAME=analyst
E2E_AUTH_PASSWORD=correct-horse-battery-staple
